name: Build & Deploy to Firebase

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Angular app
        run: npm run build -- --configuration production

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}

      - name: Deploy Firebase Hosting
        run: npx firebase deploy --only hosting --project workouttrackingcore --non-interactive

      - name: Deploy Firestore Rules (only if changed)
        if: contains(github.event.commits[0].modified, 'firestore.rules') || contains(github.event.commits[0].added, 'firestore.rules')
        run: npx firebase deploy --only firestore:rules --project workouttrackingcore --non-interactive
